衣装プロンプト工房 引き継ぎメモ（Phase 5B 完了時点）
=========================================================

■ このZIPの状態
- Phase 1〜5A・実機試行修正①・文言ホットフィックス・Phase 5B 前半（CP1〜4）・Phase 5B 後半 すべて完了
- スキーマ 0.3（0.1 / 0.2 データはそのまま読める）
- ロジックテスト 524 / 524 全通過（前半455＋後半62＋最終独立監査7）
- UI実動確認（jsdom） 48 / 48 通過
- ランダム生成監査：Claude 42,000文＋ChatGPT最終独立監査59,658文・問題 0 件
- Phase 6 は未着手のまま

■ Phase 5B 後半で入ったもの
- data/conditions.js（新規）：状態・加工 53種（汚れ10・破損13・濡れ7・血6・経年9・加工8）、
  程度3・範囲4・部位18、素材別表現（byClass）、親和データ（conditionAffinity）
- 保存構造 condition.items（type / group / severity / extent / placements、最大2件、
  3件目は「衣装の状態・加工は2件まで指定できます。」で拒否、疑似IDなし）
- app.js：SCHEMA_VERSION 0.3、normalizeCondition（未知ID破棄・既知値へ丸め・2件上限）、
  独立セクション「衣装の状態・加工」（素材・装飾の次、配色の前）、日本語構造一覧へ状態行を追加
- generator.js：blockCondition（BLOCK_ORDER の materials→decorations 間、CONNECTORS は ''）。
  matClasses 別言い換え（金属に torn/frayed/wrinkled を出さない、防水は水滴系、鱗は scales系）。
  程度副詞は with句の内側へ（with lightly chipped scales）。extent overall は throughout、
  widespread は across much of …。濡れ→透け／血→負傷・gore は一切追加しない。
  人魚では脚・靴系部位、人魚以外では尾系部位を出力しない（値は保持。インポートJSON向け防御）
- 人魚英文重複修正：mermaid_tail_form を blockParts から外し blockMerfolk へ統合。
  詳細版は「with the lower body ending in a single {尾の形}, replacing separate human legs and feet」
- advisor.js：主素材トークンへ mat_<class> タグ、condition トークン、conditionCandidates（補助候補）。
  夢かわ系（noAutoSubtypes）には候補を出さない。2件到達時は提案しない
- data/rules.js：状態系 8 ルール追加（合計38：hard5 / warning20 / info13）。
  金属×破れ・金属×しわ（warning、代替を文中で案内）、防水×水吸い（info）、金属×毛玉（hard）、
  意味重複4種（加工×破れ／補修×補修／色落ち×色褪せ／水滴×ずぶ濡れ）
- gacha.js：対象「状態・加工」（condition）。血は明示対象時のみ低確率（他対象は condition に触れない）。
  現代・制服・夢かわは状態なし高確率、性別重みなし。人魚は merfolkGachaPool から。
  破れの自動部位は tearAutoPlacements（裾・袖・片袖・袖口・膝）のみ。最大2件
- data/presets.js：グループ「状態・場面」＋状態付きプリセット8件（総数32）
- styles.css：chip / btn--sm / switch の min-height を var(--tap)=44px へ（仕様書§5-6）
- index.html / tests.html：conditions.js を decorations.js の直後に追加（app.js より前が必須）
- README：Phase 5B 完了状態へ更新（進捗表・件数・状態レイヤーの節・既知の制限）


■ ChatGPT 最終独立監査での修正
- generator.js：人魚では人間衣装由来の下半身ボリューム・丈を休止
- generator.js：着用役割、特殊パーツの動詞接続を二重にしない
- generator.js / data/parts.js：ランジェリー型の冠詞と完全な名詞句を修正
- generator.js：可算装飾を数量に合わせて単数・複数へ変化
- generator.js：宝石色の「highlights」重複を解消
- app.js：通常衣装→人魚へ切り替えた直後、状態部位を脚・靴から尾・尾びれへ即時更新
- 375 / 390 / 430pxのheadless Chromiumで横スクロールなし、表示中ボタン44px以上、3件目拒否を確認

■ 検証方法
- tests.html をブラウザで開く（524件）
- 状態確認：設計台→「衣装の状態・加工」→追加→種類を選ぶと程度・範囲・部位が展開。
  3件目追加でトースト。人魚カテゴリでは膝・靴が消え、尾・尾びれが出る
- 出力確認：泥はね汚れ＋軽度＋裾 → lightly mud-splattered along the hem

■ 既知の制約（実機で確認すべき点）
- 375 / 390 / 430px の横スクロール・stickyナビと下部バーの重なりは jsdom で計測できない。
  新UIは既存の chips（flex-wrap）と deco-item を再利用しており固定幅は追加していないが、
  実機（iPhone Safari）で最終確認すること
- chip 等の 44px 化で既存画面の縦寸がわずかに伸びる。密度が気になる画面がないか実機で見る
- 「維持する」を選んだ警告がセッション限りなのは従来どおり
